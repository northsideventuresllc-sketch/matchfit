# Cursor — retired

Cursor was retired org-wide (NI-Brain Decision #238). This `.cursor/` directory is kept here
for history only. Agents never read this folder — Claude Code sessions load `CLAUDE.md` /
`AGENTS.md` / `.claude/` instead.
