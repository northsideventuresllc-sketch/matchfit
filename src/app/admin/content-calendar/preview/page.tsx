import { ContentCalendarMockupClient } from "./content-calendar-mockup-client";

/** Public interactive UI preview — no admin auth required. */
export default function ContentCalendarPreviewPage() {
  return <ContentCalendarMockupClient />;
}
