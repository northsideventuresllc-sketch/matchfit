# SQLite (default path matches prisma/schema.prisma). A busy_timeout is appended in code for fewer "database is locked" errors during Next dev.
DATABASE_URL="file:./prisma/dev.db"

# Required for sessions and one-time codes (min 32 characters)
AUTH_SECRET="replace-with-a-long-random-secret-at-least-32-chars"

# Optional: email codes via Resend (https://resend.com). OTP mail sends From onboarding@resend.dev.
# In NODE_ENV=development, all Resend "To" addresses are redirected to northside.ventures.llc@gmail.com (hard-coded in src/lib/resend-client.ts).
# RESEND_API_KEY=

# Optional: SMS / voice codes via Twilio (production, or dev if MATCH_FIT_USE_TWILIO_IN_DEV=true)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_FROM_NUMBER=
# In NODE_ENV=development, SMS/voice OTPs are mocked (code printed in the dev server terminal) unless:
# MATCH_FIT_USE_TWILIO_IN_DEV=true
# To force terminal mock in any environment (e.g. hosted beta): MATCH_FIT_MOCK_PHONE_OTP=true

# Public site URL for Stripe Checkout return URLs (falls back to request origin if unset)
# NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Trainer onboarding UI: displayed sign-up and background check fee amounts (USD, cosmetic until billing is wired)
# NEXT_PUBLIC_TRAINER_SIGNUP_FEE_USD="99"
# NEXT_PUBLIC_TRAINER_BACKGROUND_CHECK_FEE_USD="49"

# Stripe (required for subscription checkout — use test keys in development)
# https://dashboard.stripe.com/apikeys
# STRIPE_SECRET_KEY=
# Optional if you only use hosted Checkout (no Payment Element on your site)
# NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
# Recurring Price ID for $5/month (Products → create price, recurring monthly)
# STRIPE_PRICE_ID=price_...
# Webhook signing secret (https://dashboard.stripe.com/webhooks) — endpoint: /api/webhooks/stripe
# STRIPE_WEBHOOK_SECRET=

# Dev-only (NODE_ENV=development): Mac shortcuts Command+Option+Shift+C (client) / Command+Option+Shift+T (trainer) jump into fixed test accounts.
# Use the same value you would type at sign-in: email, username, or phone.
# MATCH_FIT_DEV_CLIENT_IDENTIFIER=
# MATCH_FIT_DEV_TRAINER_IDENTIFIER=
